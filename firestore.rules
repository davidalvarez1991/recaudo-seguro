
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regla global: Permite leer y escribir en cualquier documento
    // a cualquier usuario que haya iniciado sesión.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
