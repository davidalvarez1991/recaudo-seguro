rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allows read and write access to all files.
    // This is safe in this context because the upload function is only
    // accessible to authenticated providers within the application.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
